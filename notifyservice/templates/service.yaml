apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifyservice.fullname" . }}
  labels:
    {{- include "notifyservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notifyservice.selectorLabels" . | nindent 4 }}
