apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notifyservice.fullname" . }}-test-connection"
  labels:
    {{- include "notifyservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notifyservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
