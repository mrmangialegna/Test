apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: roleBackend
  namespace: notify-service-ns
rules:
  - apiGroups: [""]
    resources: ["secrets", "configmaps"]
    verbs: ["get", "list"]